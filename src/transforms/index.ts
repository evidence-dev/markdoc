import type { Node } from '../types';
// import table from './table';

// Evidence uses a custom table tag and we dont want the default Markdoc transform
export default [] as ((node: Node) => void)[];
